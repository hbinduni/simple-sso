# Production Environment Variables
# Copy this file to .env.production and update with your actual values:
#   cp .env.production.example .env.production
# .env.production is git-ignored — DO NOT commit it with real credentials.

# Application
ENVIRONMENT=production

# JWT signing secret (REQUIRED in production — the server refuses to start without it).
# Must be at least 32 characters. Generate a strong one with: openssl rand -base64 32
JWT_SECRET=change-me-generate-with-openssl-rand-base64-32

# Microsoft Entra ID (Azure AD) OAuth — enables "Sign in with Microsoft".
# AZURE_REDIRECT_URI must be registered as a Web redirect URI on the app registration.
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=https://api.your-domain.com/api/auth/oauth/microsoft/callback

# Server
SERVER_PORT=3000
# Frontend origin allowed by CORS (your client's public URL)
FRONTEND_URL=http://localhost

# Client
CLIENT_PORT=80
# API URL for client (now runtime configurable!)
VITE_API_URL=http://localhost:3000

# Docker Compose
COMPOSE_PROJECT_NAME=monorepo

# Container Names
SERVER_CONTAINER_NAME=monorepo-server
CLIENT_CONTAINER_NAME=monorepo-client

# Docker Image Registry (GitHub Container Registry)
IMAGE_REGISTRY=ghcr.io
GITHUB_USER=your-github-username
PROJECT_NAME=simple-sso
IMAGE_VERSION=latest
