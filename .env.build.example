# Build-Time Environment Variables
# These variables are used when building Docker images locally
# Copy this file to .env.build and update with your values, then run:
#   source .env.build && make deploy

# GitHub Container Registry Authentication
export GITHUB_USER=your-github-username
export GITHUB_TOKEN=ghp_your_personal_access_token

# Docker Image Configuration
export IMAGE_VERSION=latest                    # or v1.0.0, v2.0.0, etc.
export PROJECT_NAME=simple-sso

# Note: VITE_API_URL is now a runtime variable (set in .env.production on VPS)
# No need to set it here at build time!

# Usage:
# 1. Copy this file: cp .env.build.example .env.build
# 2. Update values above
# 3. Load variables: source .env.build
# 4. Build and push: make deploy
# 5. Or build individually: make build-client && make push-client
